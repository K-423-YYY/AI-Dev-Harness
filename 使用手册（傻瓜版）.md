# Codex Harness 使用手册（傻瓜版）

> 写给"什么都不会"的新手：只要会复制文件夹、会打字，就能用。
> 本手册手把手教您每一步怎么走，每个名词都会解释，遇到任何情况都能查到怎么处理。
> 配套项目：桌面 `AI-Dev-Harness\` 文件夹（v5.0）。

---

## 第一部分：开始之前——您需要知道的事

### 1.1 这个工具是什么？

打个比方：**它像一台"自动盖房子的机器"**。
- 您只要告诉它"我要盖一栋什么样的房子"（输入目标），
- 它会自己先写一张施工图纸（计划书），
- 您确认图纸没问题后，它就会自己盖房、自己检查、自己返工，直到盖好（自动开发、验证、修复）。
- 整个过程是 AI 在干活，您只需要看结果、做决定。

### 1.2 您需要准备什么？

| 需要的东西 | 说明 |
|---|---|
| 一台电脑 | Windows 或 Linux/macOS 都行 |
| 一个"项目文件夹" | 就是要让它干活的地方。没有就新建一个空文件夹 |
| 至少一个 AI 软件 | 三选一：**Codex CLI**（本地版）/ **Codex Cloud**（云端版）/ **DeepSeek Harness（DSH）**。没有装的话，启动时会提示您，工具不会自己乱跑 |
| `AI-Dev-Harness` 文件夹 | 就是本工具，一个文件夹，复制就能用 |

### 1.3 常用名词解释（先记住这几个词）

| 名词 | 是什么意思 | 打个比方 |
|---|---|---|
| **文件夹** | 电脑里装文件的"抽屉" | 类似办公室的文件柜 |
| **项目文件夹** | 您要开发的东西所在的那个文件夹 | 施工工地 |
| **复制 / 粘贴** | 复制=把东西抄一份；粘贴=放到新位置 | 复印文件 |
| **双击** | 鼠标左键快速按两下 | 用两根手指敲门 |
| **终端 / 命令行 / PowerShell** | 一个用"打字"来操作电脑的黑窗口 | 给电脑下命令的电话 |
| **命令** | 在终端里输入的一段文字，按回车执行 | 给机器人的口令 |
| **回车键** | 键盘上的 Enter 键 | 说"执行！" |
| **AI / Agent** | 人工智能助手，能自己思考和干活 | 聪明的机器人工人 |
| **计划书（plan.md）** | 干活前写好的步骤清单 | 施工图纸 |
| **验证** | 检查干得好不好（跑测试） | 质检员 |
| **Git** | 自动存档工具，每次干活都存一版 | 施工日志存档 |
| **GitHub** | 网上的存档仓库 | 网上档案馆 |

---

## 第二部分：手把手安装（五步，每一步都教）

### 第 2 步开始前：您要能看到"文件扩展名"（推荐开启）

> 为了让您能看清文件全名（比如 run.ps1 而不是只有 run），建议开启显示扩展名：
> Windows：打开任意文件夹 → 顶部"查看" → 勾选"文件扩展名"。

### 第 1 步：找到 `AI-Dev-Harness` 文件夹

- 打开桌面，找到名叫 **AI-Dev-Harness** 的文件夹。
- 双击打开它，确认里面有这些内容（应该能看到 7 个东西）：
  `core`、`adapters`、`skills`、`templates`、`docs`、`scripts`、`sync`、`README.md`、`CHANGELOG.md`。
  （core 等是"文件夹"，README.md 等是"文件"。）

### 第 2 步：复制整个 `AI-Dev-Harness` 文件夹

1. **不要打开** AI-Dev-Harness 进入里面，而是**对着 AI-Dev-Harness 文件夹本身**。
2. 用鼠标左键点一下 AI-Dev-Harness 文件夹（选中它，会变蓝）。
3. 按键盘 **Ctrl + C**（复制）。
   - 不会用快捷键？也可以：鼠标**右键点** AI-Dev-Harness → 菜单里选 **"复制"**。

### 第 3 步：准备您的"项目文件夹"

- 打开"此电脑"（或文件资源管理器），找一个位置（比如 D 盘或桌面），
- **新建一个文件夹**，给它起个名字，比如 `我的项目`。
  - 新建方法：在空白处**右键** → 新建 → 文件夹 → 输入名字 → 回车。
- 双击进入这个 `我的项目` 文件夹。

### 第 4 步：把 AI-Dev-Harness 粘贴进去

1. 现在您已经打开 `我的项目` 文件夹了。
2. 按 **Ctrl + V**（粘贴）。
   - 或：在空白处**右键** → 选 **"粘贴"**。
3. 等一下，看到 `AI-Dev-Harness` 文件夹出现在里面，就成功了。

### 第 5 步：检查放对了没有（必须检查！）

您的 `我的项目` 文件夹里现在应该长这样：

```
我的项目\          ← 这是项目根目录
└── AI-Dev-Harness\       ← 工具放在这里（和项目根目录平级）
```

**常见错误**：如果您把 AI-Dev-Harness 打开后把里面的 core 等单独复制出来了（变成 `我的项目\core`），那是不对的。
正确姿势是：**整个 AI-Dev-Harness 文件夹**放在项目根目录里，一个不能少。

---

## 第三部分：手把手启动

### 方法 A：双击启动（最简单，推荐先用这个）

1. 进入 `我的项目\AI-Dev-Harness\core` 文件夹。
2. 找到 **run.bat** 文件（图标可能像个齿轮）。
3. **双击**它。
4. 会弹出一个黑色窗口（终端），里面会出现一个菜单，类似这样：

```
=== Codex Harness 一键开发引擎 ===
当前引擎: codex-cli　|　当前作用域: local（可用 SCOPE=global/off 切换）
1) 生成 / 查看计划书
2) 开始执行（按计划自动开发-验证-修复）
3) 修改计划书
4) 安装为全局命令（SCOPE=global）
5) 关闭影响 / 只读预览（SCOPE=off）
0) 退出
请选择：
```

5. 菜单说明（每个选项是干嘛的）：

| 选项 | 作用 | 什么时候用 |
|---|---|---|
| **1** | 生成计划书（先写施工图纸） | **第一次用，或想看计划** |
| **2** | 开始自动干活 | 计划书已确认后，正式开工 |
| **3** | 修改计划书 | 想加功能/改要求 |
| **4** | 装成全局工具 | 想在任意文件夹都能用它 |
| **5** | 只读预览 | 只想看它"会做什么"，不动任何文件 |
| **0** | 退出 | 不干了 |

6. 用键盘输入数字（比如先输 **1**），按**回车**。

### 方法 B：用命令启动（更灵活，推荐学会）

#### B-1 怎么打开终端（黑窗口）

**Windows：**
1. 打开您的 `我的项目` 文件夹。
2. 在文件夹地址栏（顶部显示路径的地方）输入 `powershell`，按回车。
   - 或者：在文件夹空白处按住 **Shift** 键 + 鼠标**右键** → 选 **"在此处打开 PowerShell 窗口"**。
3. 出现一个黑色/蓝色窗口，前面会显示 `我的项目` 的路径，比如 `PS C:\Users\你\我的项目>`。

**Linux / macOS：**
1. 打开"终端"应用（macOS 叫"Terminal"）。
2. 输入 `cd `（cd 后面有个空格）+ 把您的项目文件夹**拖进窗口**，按回车。

#### B-2 输入命令

在终端里，把下面的命令**整个复制粘贴**进去（粘贴方法：右键 → 粘贴，或 Ctrl+V），按回车：

```powershell
.\AI-Dev-Harness\core\run.bat "您的项目目标，比如：做一个带登录的待办清单网站"
```

> 注意：双引号里的中文是"目标"，换成您真正想做的事情。
> 不想要目标、只想看菜单：直接双击 run.bat 或用 `.\AI-Dev-Harness\core\run.bat`（后面什么都不写）。

### 第一次运行：会发生什么？

1. 终端会打印：`🔍 识别引擎：xxx`（自动认出您电脑上是哪种 AI 软件）。
   - 如果显示 `❌ 当前环境未适配`：说明三种 AI 一个都没装，请看[第五部分 5.1 节]。
2. 然后显示 `📋 未找到计划书，自动根据目标生成计划...`。
3. 等一下，显示 `✅ 计划已生成：...\docs\plan.md`。
4. 然后自动退出。

**现在去打开计划书看一眼**：进入 `我的项目\docs` 文件夹，用记事本（或任意文本软件）打开 **plan.md**，里面是 AI 写的施工图纸（目标、任务清单、验收标准）。

> ⚠️ 第一次运行**不会**开始干活，这是故意的：先让您看图纸、确认没问题。

### 第二次运行：开始自动干活

1. 再次双击 run.bat（或再次运行命令）。
2. 终端显示 `🚀 开始自动执行计划：docs/plan.md（最多 5 轮）`，然后开始循环：
   - `🔄 第 1 轮执行`：AI 干活
   - `🔎 [verify] 静态验证...`：质检
   - `✅ 验证通过，检查是否全部完成...` → `🎉 项目完成！`：全部搞定
   - 或者 `⚠️ 验证失败，继续下一轮修复`：返工，最多 5 轮
3. 干完后，您的项目文件就都在 `我的项目` 里了（比如 frontend、backend 等）。

### 终端里每一行是什么意思？（对照表）

| 您看到的 | 什么意思 |
|---|---|
| `🔍 识别引擎：codex-cli` | 自动认出用的是哪种 AI |
| `📋 未找到计划书...` | 还没有施工图纸，正在生成 |
| `✅ 计划已生成` | 图纸写好了 |
| `🚀 开始自动执行计划` | 正式开工 |
| `🔄 第 i 轮执行` | 第 i 次尝试干活 |
| `🔎 [verify] 静态验证...` | 检查代码质量 |
| `✅ 验证通过` | 检查通过 |
| `🎉 项目完成！` | 全部完成 |
| `⚠️ 验证失败，继续下一轮修复` | 有问题，自动返工 |
| `📝 检测到计划书修改请求` | 您要改图纸 |
| `🔕 SCOPE=off` | 只读预览模式（不碰任何文件） |

---

## 第四部分：手把手使用各种功能

### 4.1 修改计划书（加需求、改要求）

**方法**：运行命令时，把目标写成"修改计划：……"，例如：

```powershell
.\AI-Dev-Harness\core\run.bat "修改计划：增加用户头像上传功能"
```

- 关键词必须是"修改计划"、"编辑计划"或"更新计划"开头。
- 它只会改 `docs\plan.md` 图纸，不会动任何代码。

### 4.2 查看进度（干到哪了）

打开 `我的项目\docs` 文件夹：

| 文件 | 看什么 |
|---|---|
| `plan.md` | 任务清单，干完的会变成 `[x]` |
| `CHECKPOINTS.md` | 每一轮的"施工日志"（哪轮干了啥、验证结果） |
| `STATUS.md` | 如果中途停了，这里会写"为什么停 + 该怎么办" |
| `ARCHITECTURE.md` | 项目架构说明（自动记录） |

### 4.3 中途想停 / 停了想继续

- **停**：在终端窗口按 **Ctrl + C**，即可停止（进度已自动存档，不会丢）。
- **继续**：再次运行同一命令即可，它会从上次存档的地方继续干。

### 4.4 全局安装（在任何文件夹都能用）

1. 运行：`SCOPE=global .\AI-Dev-Harness\core\run.bat`（或在菜单选 4）。
2. 它会问您确认（输入 **YES** 回车）。
3. 装好后，会在 `~\.codex\` 下注册技能和命令。
4. 以后在任意文件夹，打开终端输入：`powershell -File C:\Users\您的用户名\.codex\bin\AI-Dev-Harness.ps1 "目标"` 就能用。

> 想卸载：把 `~\.codex\skills` 和 `~\.codex\bin` 里 AI-Dev-Harness 相关的东西删掉即可。

### 4.5 只读预览（SCOPE=off，安全模式）

- 运行：`SCOPE=off .\AI-Dev-Harness\core\run.bat "目标"`
- 它**不会修改任何文件**，只打印"将要做什么"的清单。
- 适合：想看看它会干嘛、或者临时不想让它动东西。

### 4.6 指定用哪个 AI（ENGINE）

正常情况下它会自动识别。想手动指定，在命令前加：

```powershell
$env:ENGINE="codex-cli"; .\AI-Dev-Harness\core\run.bat "目标"
$env:ENGINE="codex-cloud"; .\AI-Dev-Harness\core\run.bat "目标"
$env:ENGINE="deepseek-AI-Dev-Harness"; .\AI-Dev-Harness\core\run.bat "目标"
```

> 注意：如果指定的 AI 没安装，会报"未适配"，改用自动识别。

### 4.7 调整干活次数和超时

| 想调什么 | 怎么调（在命令前加） |
|---|---|
| 最多干几轮（默认 5） | `$env:MAX_ROUNDS="10"; .\AI-Dev-Harness\core\run.bat "目标"` |
| 每轮最长几分钟（默认 10 分钟） | `$env:ROUND_TIMEOUT="20"; .\AI-Dev-Harness\core\run.bat "目标"` |

### 4.8 打开 / 关闭各种功能（环境变量开关一览）

| 变量 | 设置 | 作用 | 默认 |
|---|---|---|---|
| `SCOPE` | `local` / `global` / `off` | 影响范围 | `local`（只影响当前项目文件夹） |
| `ENGINE` | `codex-cli` / `codex-cloud` / `deepseek-AI-Dev-Harness` | 指定 AI | 自动识别 |
| `MAX_ROUNDS` | 数字 | 最多干几轮 | `5` |
| `ROUND_TIMEOUT` | 数字（分钟） | 每轮超时 | `10` |
| `RUN_AS` | `planner` / `coder` / `tester` / `auto` | 让 AI 只做某类工作 | `auto` |
| `GATE_LEVEL` | `0` / `1` / `2` | 验证多严：0=只静态，1=+验收核对，2=+浏览器测试 | `1` |
| `PLAN_APPROVAL` | `1` | 开工前必须您输 APPROVED | 关 |
| `INSTALL_MCP` | `0` | 关闭自动安装 MCP 插件 | 开 |
| `AUTO_GIT_INIT` | `0` | 关闭自动 git init | 开 |
| `INSTALL_EXTRA_MCP` | `1` | 安装 Firecrawl/Context7（需 API Key） | 关 |

用法示例（PowerShell）：

```powershell
$env:PLAN_APPROVAL="1"; .\AI-Dev-Harness\core\run.bat "目标"
```

（关闭：`Remove-Item Env:PLAN_APPROVAL`）

---

## 第五部分：遇到问题怎么办（故障排查）

> 逐条对照：**您看到的 → 是什么意思 → 怎么办**。

### 5.1 `❌ 当前环境未适配 Codex Harness 工作流`

- **意思**：电脑上没装任何支持的 AI 软件（三种都没认出来）。
- **怎么办**（三选一）：
  1. 安装 Codex CLI（OpenAI 官方命令行工具），装完登录。
  2. 使用 Codex Cloud（有账号即可，需要 `codex login` 登录云端）。
  3. 在 DeepSeek Harness（DSH）环境里运行。
- 装好任意一个后，重新运行即可。

### 5.2 `未找到 codex 命令，请先安装并登录 Codex CLI`

- **意思**：引擎识别成了 Codex，但电脑上其实没有 codex 命令。
- **怎么办**：安装 Codex CLI 并登录；或者用 `$env:ENGINE="deepseek-AI-Dev-Harness"` 指定别的。

### 5.3 双击 run.bat 闪一下就没了

- **意思**：程序出错退出了，黑窗口关得太快看不到原因。
- **怎么办**：改用方法 B（用命令启动），这样错误信息会停在窗口里。

### 5.4 第一次运行只生成了计划书就退出

- **意思**：正常！这是故意设计的"先看图纸"环节。
- **怎么办**：打开 `docs\plan.md` 看一眼 → 再运行一次同一命令，就开始干活了。

### 5.5 干到一半停了，显示状态报告

- **意思**：触发了保护机制（最多 5 轮/卡住/需要您手动操作）。
- **怎么办**：打开 `docs\STATUS.md`，按里面的"可行方案"处理（1~5 条），再重跑。

### 5.6 `NEED_HUMAN: 某某操作`

- **意思**：AI 遇到只有您能做的事（比如登录账号、输入验证码、付钱）。
- **怎么办**：按提示**手动完成**那件事 → 重新运行命令，它会接着干。

### 5.7 一直 `⚠️ 验证失败，继续下一轮修复`

- **意思**：代码质量检查总不过。
- **怎么办**：
  1. 检查是不是验证命令和项目不匹配（见 6.2 微调验证）。
  2. 或者临时降低验证严格度：`$env:GATE_LEVEL="0"`。
  3. 或者把计划拆小：`"修改计划：拆分为更小的任务"`。

### 5.8 提示 `spawn EPERM` 或沙箱只读

- **意思**：AI 的工作环境权限受限。
- **怎么办**：设置 `$env:CODEX_BIN` 指向原生 codex.exe；或在 Codex 配置里把 `sandbox_mode` 设为 `workspace-write`。

### 5.9 推送到 GitHub 报 `CONNECT tunnel failed`

- **意思**：网络代理干扰。
- **怎么办**：运行同步脚本（它已内置清代理逻辑）：`.\AI-Dev-Harness\sync\sync-to-github.ps1 "说明"`。

### 5.10 所有命令都试了还是不行

- **怎么办**：运行自检脚本，它会自动检查 6 个环节并告诉您哪坏了：
  ```powershell
  .\AI-Dev-Harness\scripts\selftest.ps1
  ```

---

## 第六部分：微调（让工具更合您心意）

> 所有微调都是**改文件**。改之前建议先复制一份原文件备份。

### 6.1 新增一个技能（skill）——手把手

技能 = 给 AI 的"专业知识说明书"。想让它更懂某个领域，就加一个技能。

**第 1 步**：进入 `AI-Dev-Harness\skills` 文件夹，新建一个文件夹，名字用**英文**（比如想加"爬虫"技能，就叫 `web-scraper`）。

**第 2 步**：进入这个新文件夹，新建一个文件，**必须**叫 `SKILL.md`。

**第 3 步**：用记事本打开 `SKILL.md`，按下面的格式写内容（示例）：

```markdown
# Skill: web-scraper（网页爬虫规范）

当需要编写网页爬虫时，请遵守：
1. 使用 requests + BeautifulSoup。
2. 必须处理超时与重试。
3. 遵守 robots.txt。
```

**第 4 步**：让 AI 知道有这个技能——打开 `AI-Dev-Harness\core\AGENTS.md`，在最后的"## 技能"部分加一行：

```markdown
- 爬虫相关：调用 web-scraper 技能
```

**第 5 步**：保存所有文件。下次运行就会生效。

> 小提示：技能文件夹里可以放更多文件（示例代码等），AI 会按需读取。

### 6.2 修改验证命令（检查得更严/更松）

- 文件：`AI-Dev-Harness\scripts\verify.ps1`（Windows）和 `verify.sh`（Linux/macOS）。
- 打开后可以看到它对 `frontend` 和 `backend` 文件夹跑 `install / lint / test / build / typecheck`。
- 想改：比如您的项目只有后端，就把 frontend 那段删掉；想加 `pnpm audit` 就加一行。
- 改完建议跑一次 `selftest.ps1` 确认没改坏。

### 6.3 修改计划书模板（生成的图纸长什么样）

- 文件：`AI-Dev-Harness\docs\plan.example.md`。
- 改里面的格式（比如加一栏"预算"），以后生成的计划书就会带上。

### 6.4 修改 AI 的干活规则

- 文件：`AI-Dev-Harness\core\AGENTS.md`。
- 里面写了 AI 必须遵守的规矩（先计划再动手、省 token、每轮存档等）。
- 想加规矩：在对应小节加一行 `- 您的规矩`。

### 6.5 新增项目模板（常见项目一键生成图纸）

1. 进入 `AI-Dev-Harness\templates` 文件夹。
2. 复制一份 `iot-monitor.md`，改名成您要的类型（比如 `blog.md`）。
3. 打开它，把目标、任务、验收标准改成您的模板内容。
4. 以后想要这种项目，就在目标里写"按 blog 模板生成计划"。

### 6.6 新增适配器（支持新的 AI 软件）

> 这是高级操作，按 `AI-Dev-Harness\adapters\template.md` 里的步骤来：
> 1. 复制 `template.md` 为 `adapters\新名字.md`，填写执行命令等。
> 2. 复制 `codex-cli.ps1` 为 `新名字.ps1`，改检测函数和参数。
> 3. 在 `adapters\_detect.ps1` 和 `_detect.sh` 里加一行识别分支。
> 4. 在 `core\engine.ps1` 的 `Get-ExecCommandLine` 里加执行分支。

### 6.7 修改默认设置

- 所有默认值（5 轮、10 分钟、local 作用域等）都写在 `core\run.ps1` 开头的变量里。
- 想永久改：把 `$maxRounds = 5` 改成 `$maxRounds = 10`。
- 想临时改：见 4.7 节用环境变量，不用改文件。

---

## 第七部分：项目结构（所有文件夹和文件是干嘛的）

### 7.1 完整目录树

```
AI-Dev-Harness\                          ← 整个工具（一个文件夹，复制即用）
│
├── README.md                     ← 一页纸简介（先看这个）
├── CHANGELOG.md                  ← 版本更新记录
│
├── core\                         ← ★引擎核心（别乱删）
│   ├── run.bat                   ← Windows 启动入口（双击它）
│   ├── run.ps1                   ← Windows 主程序（真正干活的地方）
│   ├── run.sh                    ← Linux/macOS 启动入口
│   ├── engine.ps1                ← 执行循环 + 异常处理（卡住/超时/手动接驳）
│   ├── status.ps1                ← 状态报告生成（STATUS.md）
│   ├── router.py                 ← 任务路由（识别"修改计划/测试/开发"）
│   └── AGENTS.md                 ← AI 的干活规则（想改规则改这里）
│
├── adapters\                     ← ★软件适配层（支持哪些 AI）
│   ├── _detect.ps1 / _detect.sh  ← 自动识别您电脑上是哪种 AI
│   ├── codex-cli.ps1 / .md       ← Codex CLI（本地版）适配
│   ├── codex-cloud.ps1 / .md     ← Codex Cloud（云端）适配
│   ├── deepseek-AI-Dev-Harness.ps1/.md  ← DeepSeek Harness 适配
│   └── template.md               ← 新增适配器的模板
│
├── skills\                       ← ★AI 的专业知识库
│   ├── frontend-design\          ← 前端设计规范（官方）
│   ├── webapp-testing\           ← Web 测试规范（官方）
│   ├── backend-api\              ← 后端接口规范
│   └── spec\                     ← SDD 规范驱动技能（本工具特色）
│       ├── spec-writer.md        ← 教 AI 怎么写计划书
│       └── spec-checker.md       ← 教 AI 怎么核对验收标准
│
├── templates\                    ← 项目模板（常见项目一键生成图纸）
│   ├── video-downloader.md       ← 视频下载总结器模板
│   └── iot-monitor.md            ← 智能家居/远程监测模板
│
├── docs\                         ← 文档模板（只读，不动运行状态）
│   ├── plan.example.md           ← 计划书格式示例
│   ├── ARCHITECTURE.example.md   ← 架构记忆格式示例
│   └── CHECKPOINTS.example.md    ← 检查点日志格式示例
│
├── scripts\                      ← 各种小工具脚本
│   ├── verify.ps1 / .sh          ← 质量检查（lint/test/build）
│   ├── spec-check.ps1 / .sh      ← 验收标准核对
│   ├── e2e.ps1 / .sh             ← 浏览器自动测试
│   ├── setup-project.ps1 / .sh   ← 生成项目级规则文件
│   ├── update-skills.ps1 / .sh   ← 从官方仓库升级技能
│   ├── install-global.ps1 / .sh  ← 全局安装器
│   └── selftest.ps1 / .sh        ← 自检（工具坏了先跑它）
│
└── sync\                         ← GitHub 同步
    ├── sync-guide.md             ← 同步说明
    └── sync-to-github.ps1        ← 一键同步脚本
```

### 7.2 运行后，项目文件夹里会多出什么？

```
我的项目\                          ← 您的项目根目录
├── AI-Dev-Harness\                      ← 工具（保持只读，不用动）
├── docs\                         ← ★运行状态都在这里
│   ├── plan.md                   ← 计划书（施工图纸）
│   ├── ARCHITECTURE.md           ← 架构记忆（自动写）
│   ├── CHECKPOINTS.md            ← 检查点日志（自动写）
│   └── STATUS.md                 ← 停止时的状态报告（出问题时看）
├── AGENTS.md                     ← 项目级规则（自动生成，可改）
├── .gitignore                    ← Git 忽略清单（自动生成）
├── frontend\                     ← 前端代码（AI 干的活）
├── backend\                      ← 后端代码（AI 干的活）
└── .git\                         ← Git 存档（自动）
```

> 小知识：把 `AI-Dev-Harness\` 文件夹删掉，**不影响**您的项目文件；反过来，项目文件也都在项目根目录里，和工具互不干扰。

### 7.3 整个框架是怎么运转的（一张图看懂）

```
您输入目标
   │
   ▼
自动识别 AI 软件（Codex CLI / Codex Cloud / DeepSeek Harness）─── 没装 → 提示后退出
   │
   ▼
生成/读取计划书 docs\plan.md（施工图纸）
   │
   ▼
确认图纸（您看一眼；开 PLAN_APPROVAL 的话要输 APPROVED）
   │
   ▼
自动干活循环（最多 5 轮）：
   ┌───────────────► 干一轮（AI 写代码）
   │                     │
   │                     ▼
   │              质量检查（verify / 验收核对 / 浏览器测试）
   │                     │
   │             ┌───────┴────────┐
   │          通过│               │没通过
   │             ▼               ▼
   │      全部任务完成？       自动修复后回到干一轮
   │        │是
   │        ▼
   └──── 🎉 完成（每次干活都自动存档到 Git）
```

### 7.4 数据流向（谁调用谁）

```
您（双击 run.bat / 输入命令）
   │
   ▼
core\run.ps1（主程序）
   ├── 调用 adapters\_detect.ps1 → 认出用哪种 AI
   ├── 调用 core\router.py → 判断是"修改计划/测试/开发"
   ├── 调用 core\engine.ps1 → 循环干活、异常处理
   ├── 调用 core\status.ps1 → 出问题写 STATUS.md
   ├── 调用 scripts\verify.ps1 / spec-check.ps1 / e2e.ps1 → 质量检查
   └── 调用 scripts\setup-project.ps1 → 生成项目规则
```

---

## 第八部分：一页速查卡（打印/截图收藏）

```text
【启动】
  双击 AI-Dev-Harness\core\run.bat
  或：.\AI-Dev-Harness\core\run.bat "目标"

【第一次用】运行一次生成计划书 → 看 docs\plan.md → 再运行一次开工
【改需求】.\AI-Dev-Harness\core\run.bat "修改计划：……"
【看进度】docs\plan.md（勾选）/ CHECKPOINTS.md / STATUS.md
【暂停】Ctrl+C　【继续】重跑同一命令
【全局装】SCOPE=global 或菜单选 4　【安全预览】SCOPE=off
【指定AI】$env:ENGINE="codex-cli|codex-cloud|deepseek-AI-Dev-Harness"
【调轮次】$env:MAX_ROUNDS="10"　【调超时】$env:ROUND_TIMEOUT="20"
【自检】.\AI-Dev-Harness\scripts\selftest.ps1
【同步GitHub】.\AI-Dev-Harness\sync\sync-to-github.ps1 "说明"

【微调】
  加技能：skills\新目录\SKILL.md + 在 core\AGENTS.md 登记
  改验证：scripts\verify.ps1
  改规则：core\AGENTS.md
  加模板：templates\新模板.md
  加适配器：照 adapters\template.md

【支持哪些AI】Codex CLI / Codex Cloud / DeepSeek Harness（未装则提示，不执行）
```

---

*本手册对应桌面 AI-Dev-Harness\ 项目（v5.0）。如果工具升级，手册会同步更新。*
